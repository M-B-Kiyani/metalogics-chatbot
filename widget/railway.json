{
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "serve -s dist -l 0.0.0.0:$PORT --cors",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
