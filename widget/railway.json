{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "widget/Dockerfile"
  },
  "deploy": {
    "startCommand": "serve -s dist -l $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
